name: Check

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: CheckOut
        uses: actions/setup-node@v4
      - name: Install 
        run: npm ci
      - name: Lint
        run: npm run lint
